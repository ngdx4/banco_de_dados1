-- DDL
CREATE
DROP
ALTER
-- DML manipulaçao de dados
INSERT
DELETE
UPDATE
-- DQL
SELECT
-- DTL
BEGIN
COMMIT
ROLLBACK
-- DCL
GRANT
REVOKE


CREATE TABLE VAGA (
  NUMERO INT NOT NULL,
  STATUS CHAR(1) NOT NULL CHECK (STATUS IN('O','L'))
);

SELECT * FROM VAGA;
INSERT INTO VAGA(NUMERO, STATUS) VALUES(100,'O')
